{
  "name": "massingifc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Framework-agnostic kernel and plugin architecture for a federated AEC platform.",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "typecheck": "tsc --build --force",
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "devDependencies": {
    "@types/node": "^18.19.0",
    "typescript": "5.9.3",
    "vitest": "3.2.4"
  }
}
